// The project is pivoting to the spatial-board redesign (see
// docs/mechanical-spec-v0.1.md). App renders the new UI prototype.
// The previous settlement-game code remains under src/components and
// src/engine, unreferenced, until the new engine replaces it.
import Prototype from "./prototype/Prototype.jsx";

export default function App() {
  return <Prototype />;
}
