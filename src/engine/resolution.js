// Per-card effect resolution. Keyed by card id. Each resolver takes
// (state, playerId, card, opts?) and returns a new state.
// Populate as cards are implemented.
export const CARD_RESOLVERS = {};
