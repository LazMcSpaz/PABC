# v0.3 Roadmap — from "systems built" to "game finished"

**Supersedes `docs/v0.2-implementation-roadmap.md` as the live roadmap.**
That doc's phase plan (P1 Loyalty → P2 Economy → P3 Influence → P4 Fog →
P5 Diplomacy) is **done** — it's kept for its implementation detail but its
status table was stale (dated "2026-05", written before the phases it
described had actually landed) and is now corrected there. This doc starts
from the real, code-verified state and lays out what's left.

**Grounded:** 2026-08-06, against commit `9ce4a6c` (branch `main`). Verified
by reading `src/game/*.js` directly and running `node src/game/harness.js`
(265/265 checks pass, 0 failures) — not by re-reading planning docs.

## Where things actually stand

| System | Status | Notes |
|---|---|---|
| Core loop, contests, movement, attrition | **Done** | `turn.js`, `contest.js`, `actions.js`, `movement.js` |
| §17 Tech Wheel | **Done (entries) / stubbed (branches)** | 4 entry nodes have real effects and are wired into the AI (`ai.js` `maybeAssignTech`) and UI (`TechWheel.jsx`). 16 branch nodes are `effect:{kind:"noop"}` by original design (`tech-wheel-plan.md` "Out of scope") — never designed |
| §18.2 Loyalty | **Done** | Fully replaces foothold/decay (`config.js`, `contest.js`, `turn.js`) |
| §18.3 Influence / ZoC | **Done** | `influence.js`, no stubs found |
| §19 Fog of War / Vision | **Done** | `visibility.js` (per-faction explored/visible/memory + ghosts); rendered inline in `Hex.jsx` (fog dimming, `GhostToken`); AI plans on its own fog (`ai.js`) |
| §20 Economy / City Development | **Done** | Market fully retired (`economy.js`, `setup.js`, `actions.js` all confirm no live market seeding); Output/build-slider live |
| §18.4–18.13 Diplomacy | **Done** | `diplomacy.js` (~35 verbs: war, peace, pacts, vassalage, denounce, mediate, tribute, pact-calls...); Recognition victory wired (`checkRecognitionVictory`) and reachable from play |
| §15 Encounters/Quests engine | **Done, empty of content** | `encounters.js`/`quests.js`/`deferred.js`/`triggers.js` have no stubs; but `src/game/content/quests.js` and `world-encounters.js` are literally `{}` — nothing has been authored yet |
| Content editor tool | **Done** | `editor/` is a real Supabase-backed authoring app (quest graph, field encounters, chips, etc.), exports to `src/game/content/*.js` |
| Legacy settlement-game code | **Not removed** | `src/engine/`, `src/components/`, `src/hooks/` (38 files) are still in the tree, unreferenced by `App.jsx`. Dead weight, not a blocker |
| Tuning-table design pass | **Not done** | `docs/parallel-agent-briefs.md`'s "Agent 3" deliverable (`docs/tuning-tables-proposals.md`, proposed values for faction dials, Loyalty/Economy/Influence/Diplomacy constants) was never produced — current constants in `config.js` are whatever the implementing phases defaulted to, not a deliberate design pass |

**Bottom line:** every major system in the spec (§15–§20) is implemented and
exercised by the harness. The game is playable start-to-finish, including
AI-vs-AI. What's left is narrower and falls into three buckets: **AI
quality**, **content volume**, and **cleanup** — no new engine systems are
required.

## What's actually left

### 1. AI quality (engine-adjacent, not new systems)

The AI plays every system but makes some decisions naively. From
`docs/ai-overhaul-plan.md` (status corrected there too):

- **Contests are blind** — `ai.js` `tryOneAction` attacks any contestable
  target with no win-probability/EV check. No odds math exists anywhere in
  the file. This is the single most visible "the AI plays dumb" gap.
- **`training-grounds` is still hardcoded by id** (`ai.js`, build scoring +
  recruit-cap check) instead of read generically as "a chip that raises unit
  cap." Any new content chip with the same effect via a different id will be
  invisible to the AI.
- **Build/tech scoring is a hand-tuned heuristic**, not the generic
  effect→value table the overhaul plan calls for. It mostly works today
  because current content sticks to a handful of known fields, but it's not
  robust to new chip effects (movement, influence, vision, detection,
  loyalty-rate chips score 0 / get ignored).

**Recommended next step:** a focused pass adding EV-gating to contests
(don't pick a fight you're expected to lose) and delisting the
`training-grounds` special case in favor of a generic "unlocks recruiting"
effect tag. The full effect→value table rewrite is a bigger lift — worth
scoping separately if AI quality becomes the bottleneck on playtesting.

### 2. Content (the bulk of remaining work — matches the "just need content" read)

- **Quests and world encounters are empty** (`QUESTS = {}`,
  `WORLD_ENCOUNTERS = {}`). Field encounters have a real base (115 entries)
  — quests/world-encounters have zero. The editor tool to author them
  already exists and works; this is pure authoring time, not engineering.
- **Core tables are thin for a 4-faction game:** `locations.csv` (11),
  `upgrade-chips.csv` (31), `factions.csv` (4 factions), `location-abilities`
  (7), `reactive-cards` (12). Enough to smoke-test, not enough for a full
  game with variety.
- **16 tech-wheel branch nodes have no design at all** — not just
  unimplemented, literally never designed. This is content-team work
  (design what each branch does) before it's an engineering task (wire the
  effect).
- **Faction/tuning constants were never given a deliberate pass** — see
  the "tuning-table design pass" row above. Current numbers are engineering
  defaults, not balanced design values.

### 3. Cleanup

- **Remove `src/engine/`, `src/components/`, `src/hooks/`** (the legacy
  settlement-builder code) — confirmed dead (not imported from `App.jsx`
  or anywhere reachable). README's "Legacy" section already flags this as
  pending; nothing blocks doing it now.
- **Art assets** — faction emblems, terrain tiles, icons. Still a checkbox
  in the README, unaffected by everything above.
- **Retire/mark-stale the completed planning docs** so future agents (or
  future you) don't re-plan finished work — this pass updates
  `v0.2-implementation-roadmap.md`, `ai-overhaul-plan.md`, and
  `parallel-agent-briefs.md` in place with corrected status banners rather
  than deleting them, since their implementation detail is still accurate
  and useful as history.

## Suggested sequencing

Unlike the v0.2 roadmap, these three buckets are **not** dependent on each
other — they can run fully in parallel:

```
AI quality (ai.js only)         ─┐
Content authoring (editor + CSVs) ─┼─► converge whenever, no shared files
Legacy cleanup + art             ─┘
```

- **Content authoring** has the best ratio of "amount left" to "no
  engineering risk" — it's the natural place to put the most hours right
  now, and multiple people can author in parallel (quests, world
  encounters, chips, locations, tuning tables are all independent files/
  tables in the editor).
- **AI EV-gating for contests** is the highest-value engineering fix if you
  want the AI to *feel* smart while content is being authored — small,
  contained to `ai.js` + reading existing `contest.js` math, no schema
  changes.
- **Legacy removal and art** have no dependencies and can happen whenever
  someone has spare time; legacy removal is a pure deletion + smoke-test.

## Guardrails (carried over, still apply)

- Never hand-edit `content/` or `src/game/content/` — the editor tool owns
  them; author through it.
- Verify with `node src/game/harness.js` before calling any engine change
  done; it's cheap and catches regressions across every system at once.
- `src/prototype/engineAdapter.js` remains the only seam between the engine
  and the React UI.
